import { CleanCategoryNamesPipe } from './clean-category-names.pipe';

describe('CleanCategoryNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCategoryNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
